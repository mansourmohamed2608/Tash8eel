# Phase 2 - Backend Findings

| Severity | Category               | Evidence                                                                                                                                                                                                                                                                                                                          | Description                                                                                                                                                           | Recommendation                                                                                                                    | Verification                                                                      |
| -------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| P0       | Security               | `.env:22-44`, `apps/api/.env:8-15`, `apps/worker/.env:1-8`                                                                                                                                                                                                                                                                        | **Secrets committed to repo** (OpenAI key, Twilio credentials, Neon DB credentials). These are live-looking values and expose external systems.                       | Remove secrets from repo, rotate all exposed keys, and ensure only `.env.example` placeholders are committed.                     | 1) Keys rotated and old keys invalid. 2) `git grep` confirms no secrets.          |
| P0       | Security               | `apps/api/src/shared/guards/merchant-api-key.guard.ts:30-133` + `apps/api/src/api/controllers/analytics.controller.ts:12-70`, `apps/api/src/api/controllers/notifications.controller.ts:11-103`, `apps/api/src/api/controllers/early-access.controller.ts:27-160`, `apps/api/src/api/controllers/inventory.controller.ts:275-336` | **Tenant isolation bypass**: merchant-scoped endpoints accept `merchantId` from URL params without enforcing it equals the authenticated merchant in request context. | Use `@MerchantId()` or a guard to enforce path param matches authenticated merchant. Replace param usage in affected controllers. | Attempt cross-tenant request using merchant A key for merchant B and confirm 403. |
| P1       | Security               | `apps/api/src/api/controllers/early-access.controller.ts:164-205`                                                                                                                                                                                                                                                                 | **Admin endpoint is unguarded**: `/admin/early-access` has no admin guard.                                                                                            | Add `@UseGuards(AdminApiKeyGuard)` and header requirement.                                                                        | Unauthenticated call returns 401.                                                 |
| P1       | Security               | `apps/api/src/infrastructure/websocket/events.gateway.ts:47-82`                                                                                                                                                                                                                                                                   | **WebSocket auth trusts client-supplied merchantId** without JWT verification.                                                                                        | Require JWT verification or apply `WsJwtGuard` on socket connection; reject unauthenticated clients.                              | Try connecting without token and confirm disconnect.                              |
| P1       | Security               | `apps/api/src/api/controllers/twilio-webhook.controller.ts:87-97`                                                                                                                                                                                                                                                                 | **Twilio signature validation is optional** if header missing; requests without signature are accepted.                                                               | Reject when signature is missing while `TWILIO_VALIDATE_SIGNATURE` is enabled.                                                    | Send webhook without signature and confirm 401.                                   |
| P1       | Security / Performance | `apps/api/src/api/controllers/production-features.controller.ts:467-485`, `:528-601`                                                                                                                                                                                                                                              | **File uploads lack size/type limits** and log CSV data. This risks memory pressure + PII exposure in logs.                                                           | Add Multer limits (size, file count) and validate MIME/extension; remove raw CSV logging.                                         | Upload oversized file and confirm 413/400; check logs for redaction.              |
| P1       | Security               | `apps/api/src/application/services/staff.service.ts:391-415`, `apps/api/src/api/controllers/production-features.controller.ts:779-796`                                                                                                                                                                                            | **Password reset token returned to caller** (comment says “dev only”).                                                                                                | Gate by environment or remove token from response in production builds.                                                           | In production mode, reset endpoint should not return token.                       |
| P2       | Correctness            | `apps/api/src/main.ts:64-67` + `apps/api/src/api/controllers/loyalty.controller.ts:16-21`                                                                                                                                                                                                                                         | **Route path mismatch**: global `/api` prefix + controller `api/merchants/...` yields `/api/api/merchants/...`, but portal calls `/api/merchants/...`.                | Remove `api/` prefix in LoyaltyController or update portal to call `/api/api/...`.                                                | Requests to `/api/merchants/...` succeed after fix.                               |
| P2       | Data / Migrations      | `apps/api/migrations/008_production_features.sql:307-322` vs `apps/api/migrations/010_notifications_system.sql:83-119`                                                                                                                                                                                                            | **Duplicate table definitions** for `notification_preferences` with different schemas and indices.                                                                    | Consolidate into a single migration (or rename table) and add a schema migration to reconcile.                                    | Run migrations on empty DB and ensure one consistent schema.                      |
| P2       | Auth / Correctness     | `apps/api/src/shared/guards/merchant-api-key.guard.ts:35-52` + `apps/portal/src/lib/auth.ts:96-144`                                                                                                                                                                                                                               | **Portal JWTs not validated by MerchantApiKeyGuard** (TODO in guard).                                                                                                 | Implement JWT verification in MerchantApiKeyGuard (or create StaffJwtGuard) and apply to portal routes.                           | Portal authenticated requests succeed without API key.                            |
| P3       | Observability          | `apps/api/src/shared/middleware/correlation-id.middleware.ts:15-44`                                                                                                                                                                                                                                                               | **AsyncLocalStorage context not set**, so `getCorrelationId()` returns `unknown` in logs.                                                                             | Wrap request handling in `requestContext.run(...)` and set `merchantId` if available.                                             | Logs include correlationId for requests.                                          |
| P3       | Maintainability        | `docs/legacy/temp_extract/README.md:1-25`, `docs/project-scan/01_REPO_MAP.md:12-14`, `docs/project-scan/01_REPO_MAP.md:600-605`                                                                                                                                                                                                   | **Legacy code snapshot duplicated in repo** (archived copy + extracted source).                                                                                       | Move to archival storage or document purpose; exclude from builds.                                                                | Tree cleanup, builds unaffected.                                                  |
