# Phase 4 - Database Findings

| Severity | Category           | Evidence                                                                                                               | Description                                                                                                                                    | Recommendation                                                                                | Verification                                                      |
| -------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| P1       | Reliability        | `apps/api/migrations/001_init.sql:1-6` + `docker-compose.yml:3-6`                                                      | **`vector` extension required** by migration, but default `postgres:16-alpine` image does not include pgvector. Local dev migrations can fail. | Use `pgvector/pgvector` image in docker-compose or remove `vector` extension when not needed. | `docker-compose up` + migrations succeed in fresh DB.             |
| P2       | Data Model         | `apps/api/migrations/008_production_features.sql:51-92` vs `apps/api/migrations/010_notifications_system.sql:8-25`     | **Two staff tables** (`merchant_staff` and `staff_members`) with overlapping purpose and different schemas.                                    | Consolidate to one staff table, write a migration to map data, update queries.                | Migrations produce a single staff table; code references updated. |
| P2       | Data Model         | `apps/api/migrations/008_production_features.sql:307-322` vs `apps/api/migrations/010_notifications_system.sql:83-119` | **Duplicate `notification_preferences` table** with conflicting schema definitions.                                                            | Choose one schema and create a migration to reconcile, or rename one table.                   | Fresh migrations create one consistent schema.                    |
| P2       | Migration Strategy | `apps/api/src/cli/run-migrations.ts:40-120`                                                                            | Custom migration runner uses `DATABASE_URL` only and falls back to a hardcoded schema (legacy). This can drift from `apps/api/migrations/*`.   | Remove legacy schema fallback or ensure it matches current migrations.                        | Rebuild DB solely from migrations; schema matches expected.       |
