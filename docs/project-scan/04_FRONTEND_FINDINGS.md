# Phase 3 - Frontend Findings

| Severity | Category           | Evidence                                                                                                                                       | Description                                                                                                                                                                                       | Recommendation                                                                                   | Verification                                                          |
| -------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| P1       | Correctness        | `apps/portal/src/lib/authenticated-api.ts:296-405`, `apps/portal/src/components/layout/notifications-popover.tsx:34-78`                        | **Duplicate keys in `portalApi` object** override portal-level notification methods with merchant-level variants, breaking `portalApi.getNotifications()` and related calls in layout components. | Rename portal-level notification methods (e.g., `getPortalNotifications`) and update call sites. | Open portal notifications popover and confirm data loads without 404. |
| P2       | Security / Product | `apps/portal/src/lib/auth.ts:23-69`                                                                                                            | **Demo users enabled by default** in credentials provider; no explicit environment gating.                                                                                                        | Gate demo auth behind a dev-only flag or `NODE_ENV !== 'production'`.                            | In production build, demo credentials are rejected.                   |
| P2       | Correctness        | `apps/portal/src/lib/authenticated-api.ts:329-376` + `apps/api/src/api/controllers/loyalty.controller.ts:16-21` + `apps/api/src/main.ts:64-67` | **Loyalty endpoints path mismatch**: portal calls `/api/merchants/.../loyalty` but controller uses `/api/api/merchants/...` due to global prefix.                                                 | Remove `api/` from controller path or update portal routes to match.                             | Loyalty page loads tiers/promotions successfully.                     |
| P3       | Quality            | `apps/portal/package.json:1-49`, `docs/project-scan/12_SEARCH_LOG.md:29-35`                                                                    | **No portal test setup** (unit/component/e2e) found. **Not found in repository**.                                                                                                                 | Add test harness (Vitest/Jest + React Testing Library) and minimal smoke tests.                  | CI job runs portal tests successfully.                                |
